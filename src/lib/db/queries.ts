export async function getSuggestionsByDocumentId({
  documentId,
}: {
  documentId: string;
}): Promise<Array<any> | undefined> {
  // Stubbed database query; replace with real implementation.
  return [];
}